jQuery(document).ready(function($){

    //model window
    $('#wechat-modal').ezmodal({

        // the width of the modal window
        // px, 'small', 'medium', 'full'
        'width': 290,

        // is it closable?
        'closable': true,

        // auto open on page load
        'autoOpen': false,

        // callbacks
        'onShow': function () {},
        'onClose': function () {}

    });
});