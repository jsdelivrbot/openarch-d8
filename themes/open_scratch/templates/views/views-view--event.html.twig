{%
  set classes = [
    dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
%}
<div{{ attributes.addClass(classes) }}>

  <div class="card pad-widest shift-full event-details-wrapper"> {# bdt bdc--def invertable #}

    <div class="card__item card__header">

      {% include '@open_scratch/parts/card-header.html.twig' with {'title': 'Event'} only %}

    </div>

    <div class="card__body">

      {{ rows }}

    </div>

  </div>

  {{ feed_icons }}
  
</div>