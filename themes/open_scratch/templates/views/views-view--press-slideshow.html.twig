{%
  set classes = [
    dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
%}

<div{{ attributes.addClass(classes) }}>

  <div class="card press-slideshow">
    <div class="card__slide">
      {{ rows }}
    </div>
  </div>

  {{ feed_icons }}
</div>
