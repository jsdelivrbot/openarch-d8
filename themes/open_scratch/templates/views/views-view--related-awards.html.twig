
{%
  set classes = [
    dom_id ? 'js-view-dom-id-' ~ dom_id,
  ]
%}

{% if view.total_rows > 0 %}

<div{{ attributes.addClass(classes) }}>

  <div class="card coverage border--top expandable">

    <input type="checkbox" id="related-awards">
    <label for="related-awards">

      <div class="card__item card__header spaced--most expandable-head">

        <div class="media mediaTitle">

            <div class="media__imgR">

                  <div class="ribbon">
                    <div class="ribbon__segments">
                        <div class="ribbon__segment right">

                            <a class="icon icon--fa icon--button-big icon--big checked" style="background:none"><i class="fa fa-angle-up" aria-hidden="true"></i></a>
                            <a class="icon icon--fa icon--button-big icon--big unchecked" style="background:none"><i class="fa fa-angle-down" aria-hidden="true"></i></a>
                        
                        </div>
                    </div>
                  </div>
              </div>

          <div class="media__contents">
              <h4 class="title title--secondary">{{ view.display_handler.display.display_title }} ({{ view.total_rows }})</h4>
          </div>

        </div>

      </div>

    </label>

    <div class="card__item card__body spaced--most mt--0 expandable-body">
      {{ rows }}
    </div>

  </div>

  {{ feed_icons }}
</div>

{% endif %}